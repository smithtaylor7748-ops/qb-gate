// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { LongContext } from "./LongContext";

/**
 * 运行期抓回来的一条价。
 *
 * 跟 [`ModelPrice`] 是两个类型:那个是编译进二进制的快照(`&'static str`),
 * 这个是抓回来、要落盘的(`String`)。合成一个的话,内置表就没法是 `const`。
 */
export type FetchedPrice = { model: string, input_per_mtok: number, output_per_mtok: number, 
/**
 * 抓不到就留空,由 [`Catalog`] 按标准倍数补并标成 `Derived`。
 */
cache_read_per_mtok: number | null, cache_write_per_mtok: number | null, 
/**
 * 长上下文那一档的价。
 *
 * ⛔ **这是「计费翻倍」的另一种。** OpenAI 对超长上下文单独定价:
 * `gpt-5.6-sol` 短上下文 $4/$20,长上下文 $8/$30 —— 输入翻一倍。
 * 拿短上下文价当基准去验一条专跑长上下文的线,会把它冤枉成超收两倍。
 *
 * Anthropic 侧现役模型**全 1M 上下文同价**(官方原文:
 * 「900k-token 请求与 9k-token 请求同价」),所以那边恒为 `None`。
 */
long_context: LongContext | null, currency: string, source_url: string, 
/**
 * 抓回来的那天(`YYYY-MM-DD`)。
 */
fetched_at: string, };
