// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { AntigravityModelQuota } from "./AntigravityModelQuota";
import type { AntigravityQuotaWindow } from "./AntigravityQuotaWindow";

/**
 * 一个账户联网问到的额度。
 */
export type AntigravityOnlineQuota = { 
/**
 * 档位 id（`g1-pro-tier` / `free-tier`）与名字，原样。
 */
tier_id: string | null, tier_name: string | null, 
/**
 * Google 那边按 GCP 服务条款管的账户（原样转述 `usesGcpTos`）。
 */
gcp_tos: boolean, 
/**
 * 可用 AI 积分：`paidTier.availableCredits` 各项 `creditAmount` 之和。
 * `None` = 回复里没有付费档（免费档没有这一项）。
 */
credits: number | null, 
/**
 * 四格，固定顺序：Claude 5h、Claude 周、Gemini 5h、Gemini 周（缺哪格就没有哪格）。
 */
windows: Array<AntigravityQuotaWindow>, 
/**
 * 拿不到四格时说为什么（免费档 / 改版）。`None` = 拿到了。
 */
windows_note: string | null, 
/**
 * 拿不到四格时的兜底：各模型的剩余比例（跟 IDE 本机那份同一个类型）。
 */
models: Array<AntigravityModelQuota>, 
/**
 * 这次是什么时候问的，本地 `YYYY-MM-DD HH:MM`。
 */
fetched_at: string, };
