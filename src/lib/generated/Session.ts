// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { LaunchContext } from "./LaunchContext";

export type Session = { id: string, context: LaunchContext, pid: number, 
/**
 * 进程创建时间（Windows FILETIME）。**过 IPC 时是字符串。**
 *
 * 这个值现在量级在 1.3e17，远超 JS 的安全整数上限 2^53（约 9e15）：
 * 当成 `number` 传过去，`JSON.parse` 会静默把末几位抹掉。今天前端只是
 * 不用它，所以看不出症状；一旦有人把 `Session` 原样回传给后端做身份核验，
 * 抹掉的那几位会让「PID 是不是被复用了」判错，而且完全不报错。
 */
process_created: string, started_at: string, state: string, gated: boolean, detail: string, config_revision: number, };
