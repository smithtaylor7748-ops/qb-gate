# 已知限制与验证边界（v0.22.0）

本文件描述当前实现，旧版本的问题和设计经过可在 Git 历史及 DESIGN-NOTES.zh-CN.md 中查阅。

## 平台与门禁

- 当前完整支持 Windows x64。macOS / Linux 不是可用发布目标。
- 执行 ACL 只控制程序启动；不会冻结已加载进程、撤回已发送请求或隔离网络。Desktop 的 `app-<版本>` 主程序不能加执行锁，依赖受管会话和巡检。
- 巡检间隔两档统一为 5 秒（Claude Code / Codex 与桌面端同一个数），另有网络与停止耗时。IP 无法确认即发起关闭，没有宽限；网络抖动也可能关闭会话。**巡检越密，撞上探测源同时限流 / 超时的机会越多**，这类「查不到 → 关停」的误杀会随之变多，不会变少。
- **中转会话不受门禁关停。** 启动中转会话仍然要先验 IP —— 执行锁是按文件加的，不认身份，绕过解锁就等于留了一个绕过入口。但出口 IP 变了之后，看门狗只上锁并收掉官方进程，不动中转会话，会话内门禁也不写进中转环境目录。理由：中转请求打的是第三方端点、不带官方 OAuth 身份，收它换不到任何保护，却会弄丢正在写的对话。
- 启动恢复遇到结不清的记录时，只列出那几条（路径与原因），其余照常结清。可以在恢复页显式放弃它们 —— 放弃会把记录移进 `quarantine/` 保留，相关配置文件保持当前状态，不再能按那条记录回滚。
- **面板托管的会话（含中转、账户页起的 GPT 桌面端、以及被移出门禁的 GPT）在面板退出时会被系统一起终止**（Job Object 绑定）。请最小化到托盘以继续使用。这是刻意的：不绑定的话，命名 Job 对象会随面板退出消失，进程还在跑而面板已经认不出它 —— 界面会把它报成「已退出」，此后既停不掉，切账户清场时还可能把它当成官方进程一起杀掉。未受管程序与宿主保护链中的程序可能无法被关闭，界面会报告失败。
- Machine-wide 安装目录可能需要管理员权限才能修改 ACL。部分失败不会被报告成完整成功。
- 第二次启动会唤起已有面板。不同旧版本使用不同名称时，仍应先卸载旧面板避免并存。

## 客户端与扩展

- Claude Desktop 仅官方启动。Codex 官方登录不参与 Claude 槽位切换。当前 Codex 启动使用 Responses；Chat Completions 可诊断，但不能作为当前 Codex 启动协议。
- **「Codex 出站与换出口」插件是外部程序，本面板只接入。** 需要你自己从它的仓库 Releases 下载发布包解压（默认找 `%LOCALAPPDATA%\Programs\ccodex-sleep-state\ccodex-sleep-state.exe`，也可在插件页指定）。它接管的是**当前激活的 Codex 账户槽位**目录（没有槽位才是默认 `~/.codex`；插件页「接管的 Codex 目录」写着是哪份），接管期间别切槽位。与账户页的官方 turn-state 识别**双向互斥**：0.24.7 起两者都要改同一个槽位的 `config.toml`（识别改 `model_provider`，插件整份接管），同时开就是两个程序抢同一个文件——识别开着时后端拒绝启动插件（到「识别」弹窗点「关闭识别」），插件跑着时拒绝开启识别。它以带窗口的独立进程运行、不随面板退出而结束；**面板重启后认不出之前起的那份**（命名 Job 随面板消失），只会报「端口被占、不是本面板起的」，那时请到它自己的窗口按 Ctrl+C（它会恢复 Codex 配置）。面板的「停止」= 结束进程 + 跑它自己的 `restore`；`restore` 没成功时会原样报出，请到它的面板用「检查与修复配置」。
- **账户页的「识别」入口在窄窗口（≤780px）下收起。** 那一页是固定高度，680 这一档标题栏放不下再多一个按钮（test:ui 钉着不许裁切）。同一个弹窗在中转站页的 Codex 分页仍能打开。
- **Codex（Microsoft Store 版）自动更新后可能需要管理员重新注册，否则任何启动都被系统拒绝（拒绝访问 / os error 5）。** 新版 Codex 的打包应用带了需要管理员注册的服务组件；Store 后台更新一旦没走完，当前用户对该包的注册就失效，此时 `std` spawn、`CreateProcessW`、应用激活（`IApplicationActivationManager`）**全都**返回访问拒绝（激活 API 直接报 `0x80073D28 administrator privileges required`）—— 与本工具用哪种进程创建方式无关，换成 CreateProcessW 也一样。账户页会把这种情形说清楚，并提供「修复 Codex 注册（需要管理员）」一键修复（`Add-AppxPackage -RegisterByFamilyName`，会弹 UAC）；也可在 Microsoft Store → 库 更新 Codex。修复需要管理员，面板无法在不弹 UAC 的情况下自动完成，使用者不授权就修不了。
- API 服务的模型、工具与流式兼容性以实际响应为准。后端来源线索不等于身份认证或安全背书。
- Skills 更新检查解析上游固定版本；MCP 精选条目的版本随程序目录发布，手动 MCP 通过重新导入更新。MCP 配置对象包含多个服务时按填写的名称选择一个条目，不自动全部安装。
- 酒馆集成优先接入已有安装。应用本体与其依赖更新仍由原项目管理；QB Gate 提供接入、运行、桥接与资产备份管理。未登记的已占用端口不会被冒充为受管进程。
- 酒馆「自动定位」只扫固定盘与家目录，跳过系统目录、`node_modules`、`.git` 这类重目录与隐藏目录，也不碰网络盘和可移动盘。默认档深度 4，装在更深处的要点「深扫」（深度 7），再找不到就自己填路径。预算用完时界面会写明「没扫完，结果可能不全」—— 那跟「没找到」不是一回事。候选一律由使用者点「采用」，面板不自动写配置。
- Skills 更新遇到文件修改会阻断，需先保留修改；卸载保留文件。配置快照不包含完整扩展资产，恢复后按实际文件重新核验状态。
- 当前程序会话内保留筛选与草稿；退出面板不持久保存含 Key 的草稿。新操作记录可跨重启查询；旧安装服务的细粒度进度仍使用兼容事件，重启后不会恢复完整逐行进度。
- 历史记录有上限：操作 200 条、会话 100 条、诊断 50 条，超出的按时间清掉（正在运行和待核验的会话永不清理）。硬盘上的配置恢复记录保留 30 天；仍被某个环境「回滚配置」指着的那条不受保留期影响。超过之后旧记录不可再回滚。
- Skills 只能装到 Claude Code：Codex 不读 `skills/` 目录，装进去不会生效，所以界面不提供这个选项。
- 「检查来源更新」不会改动精选目录里写死的版本 —— 那几个提交哈希是 ATTRIBUTION.md 里核对过许可证的那一版。它只报告上游有没有新提交；要用新版自己从来源手动导入。

## 账户与用量（v0.20.0）

- **没有「管理账户」页了**（v0.20.0）。删除直接长在总览的槽位横条上，`/accounts`
  重定向到 `/`。那一页独有的「官方目录里的 API 配置」搬进了总览的折叠块。
- **用量里的美元是「按官方 API 价折算」，不是账单**（2026-09-24 起；在这之前只给「缓存省下的」）。
  回答的是「同样的 token 走 API 要付多少」；订阅账户付的是月费，不按这个收费 —— 界面把这句写在数字旁边。
  单价来自内置的官方价快照或本次启动抓到的定价页，「按什么价算」那张表标明是哪一种；
  认不出价的模型不计入，单独列出模型名、条数与 token 数。
- **缓存命中率的分母里没有输出**（`缓存读 ÷（输入 + 缓存读 + 缓存写）`）——
  输出是生成出来的，没有「命中」一说。一个字都还没读过时显示「—」而不是 0。
- **槽位列表一页 3 条**，多的走页码：这一页要在默认窗口里一屏放完。
- **删槽位不可恢复。** 删掉的是整个登录目录：凭证、设置、会话历史一起没。那个账户
  以后要用得重新登录一次。**当前激活的槽位删不了** —— 删它会留下一个指向空处的
  `claude-profile`，而没有任何东西会去修它，下次启动 Claude Code 会安安静静退回
  默认目录 `~\.claude`，界面上什么都不报。先切到别的账户再删。
- **「还能用吗」会带着你的官方令牌对外发一次请求。** 打的是公开的
  `/v1/models`（不打模型、不产生 token、不问额度、不写回任何东西），
  只在你点的时候发。本地访问令牌已过期时**不发** —— 那种时候被拒是必然的，
  报出来只会是假警报；面板也不会拿 refreshToken 去换新令牌。
  ⚠ 这个端点认不认 Claude Code 的 OAuth 令牌**还没用真令牌验过**，
  所以「被拒」那一档写明了两种可能，面板分不开。
- **token 统计的归属有三级依据，都是官方客户端自己写在本机的东西**（0.25.0）：
  ① 桌面端资料目录里的会话记录 `claude-code-sessions\<账户>\<组织>\local_*.json`
  （`cliSessionId` 对上转写文件名）；② 桌面端「无文件夹」会话的项目目录名里嵌着的
  账户 uuid；③ 旧转写里 `bridge-session` 行的 `ownerAccountUuid`。
  **第 ③ 级从 Claude Code 2.1.271（2026-09-15）起就不再被写入**，0.21.0–0.24.8
  只认它，所以那之后桌面端跑的会话全被算成「未归属」，用量卡永远是 0。
  三级都归不出的记录仍然**不摊给任何账户**，单独显示成「归不到任何槽位」——
  多半是终端里直接起、没经过面板的旧会话。桌面端里删掉的会话记录也会让对应转写
  退回这一档。
- **token 统计覆盖两处**：槽位自己的 `projects\`，以及默认目录
  `~\.claude\projects` 里归得上这个账户的那部分；两处都包括子代理转写
  `<会话>\subagents\*.jsonl` 与工作流子代理 `<会话>\subagents\workflows\<wf>\*.jsonl`（都归父会话）。
  仍然不是「这个账户一共用了多少」—— 归不出属的那部分单独列，别的机器上跑的更不在内。
- **GPT 槽位的用量只数 `home\sessions` 里的本机会话记录。** 桌面端的云端任务
  不写本机文件，只用过云端任务的槽位就是 0 份文件、0 个会话 —— 页脚会写明
  「该槽位还没有本机会话记录」，不要读成统计坏了。
- **token 统计必须去重，而且去掉的那一半和留下的一样多。** 续接会话与自动压缩会把
  之前的回复原样重放进新的转写文件。实测一个槽位 6,049 条记录里 2,860 条是重放。
  去重键是 `message.id` + `requestId`；同一个键出现多行时留**最完整的一份**（有用量 > 带 `stop_reason` >
  输出大 > 时刻早，2026-09-24 起，参照 cc-switch）。跟别的工具对不上数时，先看两边去没去重。
- **用量读数的来源与新旧要一起看。** 桌面端的样本只在桌面端运行时才写，关掉之后读数
  就冻在最后一条；Claude Code 留下的快照经常更旧。跨过窗口长度的读数一律显示
  「读数已过期」而不是那个数字，**两个来源同一把尺子**。
- **剩余天数查不出「被风控下线」。** 它只读本地那个时间戳。令牌被回收时凭证文件还在、
  到期日还是将来某一天，而实际已经发不出请求 —— 唯一能确认的办法是实际发一次认证请求。
  所以详情页的「重新登录」是常驻的，不按到期与否隐藏。

## 出口一致性（v0.20.0，综合评分第五项）

- **它只收与出口 IP 对不上的信号。** 浏览器里的 claude.ai 痕迹、扩展权限、
  凭据管理器、环境变量残留都不在这一项里 —— 它们跟出口没关系，分别在软件页的
  Chrome 卡片和「中文环境」详情里的本机体检。
- **浏览器那三项默认只看 Chrome。** Edge 与 Firefox 不在范围里，跟软件页那张 Chrome 卡片一样。
  例外（2026-09-24）：一小时内用「用默认浏览器测」实测过的话，「浏览器语言」「WebRTC」两行改用那次实测，
  哪个浏览器打开的链接就算哪个；DoH 那一行仍只读 Chrome 的策略。
- **注册表里写进了策略不等于 Chrome 已经用上。** 面板读不到 `chrome://policy`，
  改完请重启 Chrome 自己核对。面板只写 HKCU；整机（HKLM）压着的同名策略不是面板设的，
  一个字都不碰，但会把来源报出来。
- **DoH 只设到 `automatic`，不设 `secure`。** `secure` 要求同时给出
  `DnsOverHttpsTemplates`，没配的话 Chrome 什么都解析不出来。那个解析器该填谁，
  只有你自己知道。
- **浏览器语言与出口那一项只报告，不修。** 面板不替你改浏览器或系统的身份 ——
  那是 DISCLAIMER 与 CLAUDE.md 点名不做的设备指纹伪装。
- **判不出浏览器里登的是哪个账户。** 只能报「有 claude.ai 的站点数据」。
- **扩展只列装着的，看不出有没有启用**，也不提供停用或删除的入口 —— 那该在浏览器里
  自己决定。
- **「查不了」的行不参与评分**，分子分母里一起去掉 —— 算 0 分是在冤枉使用者，
  算满分是在替一个没做过的检查打包票。七行（2026-09-24 起；原来五行）全都查不了时这一项整个不计入总分。
- **浏览器语言那一项只判一种组合**：中文语言 + 非中文地区出口。别的组合不下判断 ——
  说不清楚的信号不如不说。

## 中转站（v0.14.0 起）

> **⚠ 中转站（含智能调度）目前仍在内测：界面里已经出现，但还不能正常使用。**
> 请不要把它用于实际工作，也不要依赖它给出的排序、倍率或检验结果。
> 下面各条记录的是实现上的边界，不代表这些功能已经可以放心使用。
> 使用第三方中转站的合规风险见 [免责声明 §5.1](../DISCLAIMER.md)。

- **本机路由只绑 `127.0.0.1`。** 它只转发明确指向它自己的请求，不修改系统代理设置、不接管其他程序的流量、不做链式转发。它不是代理或翻墙工具，也不会让你访问到网络本来访问不到的地方。详见 [免责声明 §5.1](../DISCLAIMER.md)。
- **中转站的熔断只在你自己填的线路之间切换，永远不会触发官方账户槽位切换。** 两条路径在代码上隔死（`qb-station` 不依赖 `qb-accounts` / `qb-launch`），由 `src-tauri/tests/architecture.rs` 的测试钉着。
- **站点检验会计费，只在你点的时候跑。** 六项里有两项**永远记「没测到」**：上下文窗口与最大输出。要测它们得构造超长输入和超大 `max_tokens` 去试探，而「试到多少算到顶」的判据没有定过，每试一次都真花钱。记「没测到」会同时压低可信度和证据档次 —— 这是有意的，没核实就不该读作可信。
- **0.23.1 查套路范围**：后台账号密码或独立浏览器登录，New API / Sub2API 自动取得会话。默认六次受控请求，可另行确认第七次冷前缀对照；整轮倍率须完整且相同请求 ID 的新账单。缓存复用扣除背景，不鉴定模型身份或全站成功率。健康度最多最近 100 条。Linux DO 通过本站登录页完成，原压缩包缺少其 Node broker，本项目使用独立 WebView2 窗口。真实站点登录及计费运行需本人凭证。见[使用与修复说明](RELAY-REPAIR-0.23.1.zh-CN.md)。
- **可信度必须和证据档次一起看。** 同样 29 分，可能是「六项都测了、四项对不上」（该换站），也可能是「只测到一项」（该再跑一轮）。只看分数分不出来。
- **检验出对不上不等于拉黑。** 面板只把真实倍率算出来参与排序，没有「排除 / 禁用」按钮。
- **官方参考价没有 API，只能抓两家的定价文档页。** 页面改版时解析器可能只认出一两行，那种「半成功」会用残缺的表盖掉完整的内置快照，所以解析不足 3 个模型就整批丢弃、沿用快照，界面照实说明用的是哪一份。缓存两项除非官方单独标过，否则按标准倍数推算，`PriceBasis` 标明是哪种。
- **分组不能自动发现。** `/api/pricing` 能拉到模型和它们的分组名，但把分组列表拉下来**建线路**需要登录站点后台，没做。线路仍需手工添加（添加弹窗会顺手建站点，站点名撞上已有的就复用那一个）。
- **`peak_rate` 没有数据来源**，恒为 `None`（按不修正处理）。~~`group_ratio` 没有数据来源、`/api/pricing` 不返回~~ —— 这句是错的：New API 把分组倍率放在 `/api/pricing` 响应的**顶层** `group_ratio` 里，0.25.4 起按线路的分组读（sub2api 那边登录后读 `/api/v1/groups/rates`）。见下面 0.25.4 那一节。
- **站点没公布 `completion_ratio` 时界面什么都不显示。** 「不知道」不等于「没加价」。⛔ 公布了、而且大于 1 **也不等于「翻倍」**：它是「输出价 ÷ 输入价」，Claude 官方本来就是 5；0.25.4 起只在它比官方的比例多出 5% 以上时挂「输出加价」。

## 中转站（v0.18.0 追加）

- **桌面端启动前必须先退干净。** 它是 Electron 单实例：已经在跑的时候再点「启动」，只会把旧窗口拉到前面，新给的数据目录一个都不生效 —— 面板会拦下来并说明，但这一步没法替你做。
- **桌面端走的是它自己的「第三方网关」部署模式**（`deploymentMode: 3p`）。这是桌面端给企业自建网关准备的功能，本项目只是把端点指到本机路由。**上游哪天改了这套键名或者收回这个模式，面板这边不会知道** —— 症状会是桌面端照常起来、照常走官方。
- **桌面端那份配置里不写模型清单**（`inferenceModels`）。模型由本机路由转发 `GET /v1/models` 从站点取；填错了反而会盖掉站点真正提供的那一份。要指定的人自己改 JSON。
- **面板不碰 `disableDeploymentModeChooser` 和 `coworkEgressAllowedHosts`。** 前者是「切回官方」那个开关，后者是 cowork 的出网白名单 —— 中转站要的只是换个推理端点，不顺带放宽别的。所以桌面端里那个部署模式选择器**仍然看得见**，你随时能自己切回去。

## 中转站（v0.17.0 追加）

- **智能调度只在面板开着时生效。** 循环活在面板进程里，一跳 60 秒。关掉面板它就不再换上游了，而客户端还在照着上一次选的那条线跑 —— 可能是几天前那条已经涨价的。界面上写着这句话；这不是缺陷，是这个软件的定位（它是个面板，不是后台服务）。
- **本机路由的端口写死默认值 15721。** 走本机路由的那个环境把这个端口写进了磁盘上的配置文件，所以从中转站页面启动时也只用默认端口。手动用别的端口起路由，客户端会连不上，而它报的只是「连不上」。要支持自定义端口的话，端口得先变成环境自己的字段。
- **请求日志那一列没有 token 数。** 本机路由不解析响应体（流式响应要整个缓冲才拆得出用量，代价太大），所以逐条记录里 token 那一格是 `—`。分类 token 只有站点账单里有 —— 弹窗里那张卡标着「站点账单口径」，跟左边「路由日志口径」的成功率对不上是正常的。
- **六个快捷开关的键名和判定条件都对着 cc-switch 的源码核过了**（`CommonConfigEditor.tsx`），模型映射对的是 `useModelState.ts`。仍然要说明的是：这些是 **Claude Code 自己的**环境变量，它们各自做什么、哪一版开始有，取决于客户端而不是这个面板 —— 上游改了名字，写进去的那一行就会变成一句没人听的废话，而界面上看起来一切正常。`Teammates 模式`、`Tool Search`、`最大强度思考`、`禁用 Artifact` 是实验/兼容性开关，属于这一类。
- **「声明支持 1M」不是一个环境变量**，是在模型名末尾加 `[1M]` 后缀（cc-switch 的 `CLAUDE_ONE_M_MARKER`）。认不认这个后缀由**中转站那边**决定 —— 面板只负责把它写进配置，站点不认的话没有任何提示。
- **协议探测发的是故意不合法的空请求体。** 上游连模型都不碰，一个 token 都不花。代价是它只能证明「这条路径在」，不能证明「这条路径真能跑通你的模型」—— 后者要真发一次请求，那是站点检验的事。Key 或站点的问题（401 / 403 / 429 / 5xx）一律留「不知道」，**不猜成「不支持」**。

## 完全卸载（v0.19.0）

- **「清干净了」只能证明到面板扫得到的那些位置。** 复扫走的是同一套扫描器，所以它证明的是
  「这些位置现在是空的」，**不是**「这台机器上再没有任何痕迹」。面板够不到的地方见下面两条。
- **面板只看得见 Windows 这一侧、当前这个用户。** 每个 WSL 发行版里的安装与配置、其它
  Windows 用户账户下的副本，都扫不到也删不掉（扫别人的私有目录本来也不该由面板擅自做）。
  卡片里那份卸载提示词就是兜这一块的：它带着已扫到的完整路径清单，交给别的 AI 去补。
- **正在运行的 exe 删不掉。** Windows 允许改名正在运行的可执行文件，但不允许删除它。
  Claude Code 与桌面端在动手前会先被一键关闭；**Codex 不会** —— 面板不按进程名杀进程
  （档案 §1 第 5 条），所以正在跑的 Codex 要自己先关掉，否则它那几项会出现在复扫的剩余里。
- **删账户槽位 = 所有账户都要重新登录。** `claude-profile-*` 是 Claude Code 的登录目录，
  酒馆桥接指向的是同一份，所以它也会跟着失效。删完「官方账户」页会回到新手引导。
  这是使用者定的口径（「要清理就全部清理掉」），不是误删。
- **认证、环境变量、Shell 配置行只报名字与位置，不报内容。** 盘点结果里看不到 Token、
  密钥或那一行原文 —— 结构上就装不下（跟 `sysenv::SecretHit` 同一条原则）。
  所以确认框里那几项只能看到「变量名」「文件:行号」，这是有意的。
- **PATH 与 Shell 配置是合并成一次改写的。** 同一个文件只读写一次，中途失败就整体算失败，
  不会留下改了一半的 profile；代价是那一类要么全成、要么全不成，没有逐项结果。
- **卸载不负责装回来。** 完全卸载之后这台机器上就没有这个软件了，要重新用得自己去
  「安装」那一步。Chrome 是唯一的例外：它另有「清空并重装」。

## 启动对齐（v0.19.0）

- **「已经一致」时什么都不做，所以开着开关也可能一次动作都没有。** 这是有意的：
  切时区要弹 UAC、改显示语言要注销，在已经一致的机器上做这些等于每次开面板
  骚扰使用者一次。每一项做没做、为什么没做，审计日志里都有一行。
- **显示语言默认关，而且设了也未必生效。** 它要先装好对应语言包，并且**要注销之后
  才生效**。面板只负责调 `Set-WinUILanguageOverride`，设完系统认不认由系统说了算 ——
  **面板无法证明它真的生效了**，界面上也不会假装知道。
- **区域格式只对之后新启动的程序生效。** 已经开着的程序（包括面板自己）读到的还是
  旧值，所以改完当场看界面是看不出变化的。
- **切时区要管理员权限。** 拿不到就是切不成，这时日志里会写「系统时区没切成」并带上
  原因，**不会**静默当成成功。
- **对照表只收常用时区。** 查不到对应的 Windows 时区或区域标签时一律跳过并记下原因，
  不猜 —— 猜错会把机器设成一个使用者没要过的地区，比不改糟得多。所以小众出口地区
  可能出现「开关开着、日志里写着跳过」。
- **首次运行不对齐。** 面板还没就绪的那一档整段不执行，否则会在使用者还没同意过任何
  事之前弹一个 UAC 去改系统时区。
- **这不是「防封」。** 对齐只是让 Claude Code 读到的时区与格式跟出口一致。
  DISCLAIMER 写死了「去除中文环境特征不会降低任何封禁概率」。

## 浏览器出站锁与系统代理（v0.19.0）

- **⚠ 防火墙规则不随面板退出而消失。** 它是系统级持久对象：面板关掉、甚至卸载之后
  规则仍然在，浏览器会继续上不了网，而使用者多半想不到是这里。
  撤销入口在面板里，**卸载面板之前请先撤销**。
- **拦的只是你点名的那几块网卡。** 没点到的接口一概不受影响 —— 这是有意的
  （需求文档要求「不对未知接口、远程桌面、局域网共享或系统服务做广泛阻断」），
  但代价是**选错网卡就等于没拦住，而面板并不知道你选错了**。面板只如实列出网卡名
  与驱动描述，不替你判断哪块是物理网卡、哪块是 TUN —— 猜错的两种后果
  （拦到 TUN 上当场断网、漏掉物理网卡等于没拦）都比让你自己选糟。
- **加规则要管理员权限。** 拿不到就是加不上，会如实报错。面板自己**不提权**
  （安装是 `currentUser` 模式，没有 requireAdministrator 清单），所以想用这个功能
  得以管理员身份启动面板。弹窗里写了这一条。
- **网卡名里带 `*` 时，规则可能落到不止那一块网卡上（v0.19.2 起的已知缺口）。**
  Windows 自己就把 WiFi Direct 那几块虚拟网卡叫「本地连接\* 1」/
  「Local Area Connection\* 2」，而 `New-NetFirewallRule -InterfaceAlias`
  接受通配符。**删**的那一侧已经转义（`QB Gate` 只删自己算出来的那个名字），
  **加**的那一侧没有：不确定防火墙的接口过滤器认不认反引号转义，而猜错的代价是
  规则落在一个匹配不到任何网卡的别名上 —— 一条看起来加上了、实际什么都没拦的规则，
  正是这一版在修的那类毛病。**查不到就不猜**，先如实记在这里。
- **出站锁不等于「不会泄露」。** 它挡的是经过那几块网卡的**新建出站连接**；
  已经建立的连接、其它浏览器、其它程序都不在这条规则的范围内。
  面板不会、也不能给出「绝对无泄露」这种结论。
- **面板只碰自己加的规则。** 判据是规则名前缀 `QB Gate - `；不带这个前缀的一律不动，
  包括你自己加的同类规则。
- **⚠ 改系统代理可能当场断网，而且影响整机。** 那是**整机**设置，所有跟随它的程序
  都会跟着走，不只是那一个浏览器。面板会拦下一种明显自毁的组合（开着代理却没填地址），
  但拦不住「地址填错了」。
- **改完之后已经在跑的程序多半要重启才会读到新值。** 面板改完会读回注册表核对，
  但那核对的是**设置本身**，不是「所有程序都已经用上新设置」。
- **原值存在 `%LOCALAPPDATA%\ClaudeIpGate\proxy-backup.json`。** 连续改两次时，
  备份保留的是**面板第一次动手之前**那一份；还原成功后备份会被清掉，
  「还原」按钮也随之消失。

## 浏览器隐私面审计（v0.19.0）

- **只列「装着的」扩展，判不了有没有启用。** 那要解析 Chrome 的内部状态文件
  （`Secure Preferences`，还带完整性校验），判错就是一句说谎的结论。
  请自行到 `chrome://extensions` 核对。
- **只报告，不动手。** 面板没有禁用或删除扩展的入口 —— 这是需求文档的硬要求，
  也是有意不提供的：没有那个功能，就不会有人误用它。
- **权限判定宁可多报也不漏报**，所以可能出现误报。漏报的代价是面板说
  「这个扩展没问题」，比多提醒一次糟得多。
- **扩展名用了 `__MSG_xxx__` 国际化占位时会留空。** 真名要去 `_locales` 里查，
  面板不编一个名字顶上。
- **⛔ 注册表里有值 ≠ 策略已生效。** 面板读不到 `chrome://policy`，所以设完
  WebRTC 策略只能告诉你「注册表里有了这个值」。要确认 Chrome 真的用上了，
  **请重启 Chrome 后自行到 `chrome://policy` 核对**。
- **面板写的是当前用户（HKCU）的策略。** 如果整机（HKLM）还压着一条同名策略，
  Chrome 以整机那条为准 —— 那不是面板设的，面板一个字都不碰，
  但审计结果里会标出这条策略的来源。
- **只看 Google Chrome。** Edge、Firefox 及其它浏览器不在审计范围内，
  跟「清空重装只认 Chrome」是同一条边界。

## 软件页（v0.19.2）

- **卡上不显示「本机有几处」，直到你点过「完全卸载」。** 那个数字要真跑一次只读盘点
  才有；四张卡一进页面各跑一次的开销不能接受，而编一个数字更糟。点了之后先盘点、
  把每一处连同归属依据摆出来，才谈删。
- **「重新扫描」会顺带记录新手引导里「环境」那一步的结果。** 页底原来那个
  「记录本步结果」按钮去掉了，但记录这件事没丢，只是挪到了扫描成功之后。

## 升级残留

- **`%LOCALAPPDATA%\QB Gate\export-types.exe`（0.12.0 留下的）。** 那一版把开发期的类型
  导出工具当成 bin 目标打进了安装包。0.13.0 起它挪到了 `src-tauri/examples/`，
  `cargo build --release` 不再产出到 `target/release` 根目录，**新装的不会再有这个文件**。
  但**就地升级不会删掉它** —— 从 0.12.0 一路升上来的机器上还留着。手动删掉即可，
  它不参与运行。

---

## 验证范围

自动回归使用临时目录、虚构配置和模拟数据，不登录真实账户、不调用付费模型、不修改用户软件或实际运行期目录。UI 回归验证五个入口、主题、窄窗口、缩放与键盘流程；演示截图不是本机 IPC、真实供应商或 OAuth 登录的证明。

发布时请结合 `CLAUDE.md`「收工清单」那十项的实际结果评估（前端六项 + Rust 四项），特别是系统权限、第三方客户端升级、真实多会话运行和异常断电场景。安装包没有配置代码签名；发行时提供源码标签和 SHA-256。

## 代码签名 / 杀软误报（0.29.0 核对）

**现状：没有代码签名，而这就是被 Defender 反复隔离的根因。**
本机 2026-09 **四次** `Trojan:Win32/Bearfoos.A!ml` / `B!ml`（装好的 `qb-gate.exe` 与
0.24.7 的 `*-setup.exe` 都被隔离过；最近一次 2026-09-22 06:05）。
`!ml` = 机器学习启发式，不是特征码命中。处置办法、申诉步骤、
0.29.0 减少了哪些触发信号、以及**哪些办法对别人起作用**，都在
[ANTIVIRUS.zh-CN.md](ANTIVIRUS.zh-CN.md)。

⚠ 0.29.0 那几项缓解**只能降低概率**。每次 `tauri build` 出来都是一个新哈希、
零信誉的文件，随时可能再被报一次。

⛔ **2026-09-22 的连带事故**：exe 被隔离后去 GitHub 下了当时的最新 release（v0.24.8，
那时 GitHub 落后本机好几版），装上直接卡在「需要完成数据恢复」页。杀软误报的真实代价
里要算上这一条：**它逼着人去降级**。0.25.3 起 GitHub 与本机发布同步、而且面板会弹窗提醒新版，
这条只在「本机装过比 GitHub 更新的构建」时还成立。

### ⚠ 先纠正一个预期（2026-09-22 核对）：没有「花钱买立刻见效」这条捷径了

**EV 证书从 2024 年起也不再给即时 SmartScreen 信誉**，微软把那个特殊待遇取消了；
OV / EV / Azure 现在走的是同一套有机积累
（<https://learn.microsoft.com/en-us/windows/apps/package-and-deploy/smartscreen-reputation>）。
所以**别花几百刀买 EV 换立竿见影**。

签名真正的价值是另一件事：**信誉可以跨版本继承** —— 同一个签名身份连续发布，
后面的版本继承前面攒下的。现在每一版都是从零开始，这才是根本差别。
它也因此跟 `CLAUDE.md` 那条「版本号只在出包那一刻提」绑在一起。

### 各条路的代价（2026-09-20 核对，2026-09-22 补两行 + 三条代价）

| 路线                       | 能不能走          | 代价                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| -------------------------- | ----------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **SignPath.io 开源免费档** | ⛔ **现在不合格** | 条款要求 "an OSI-approved Open Source license **without commercial dual-licensing** for all components"（<https://signpath.org/terms.html>），而本仓库有 `LICENSE-COMMERCIAL.md`（README 第 92 行引着它，开头第一句就是「QB Gate 采用**双授权**」）。要走这条得先**永久放弃**商业授权那一档。另有一条软风险：条款排除 hacking / 安全漏洞类工具并限制「侵犯隐私的功能」，而面板扫 Chrome `Cookies`/`History` 字节、写 Deny ACE、杀第三方进程、改代理与防火墙 —— **审核不保证过**。 |
| **Certum 开源开发者证书**  | ⛔ **不合格**     | 约 $50 首年（续期 €29），便宜到值得一查 —— 但卡两条：① 条款写明「若认定证书被用于**商业分发**的软件则吊销」，跟 SignPath 是同一堵墙；② 证书 CN 强制写 `Open Source Developer` + **你的真实姓名**，要交身份证件与账单地址。                                                                                                                                                                                                                                                       |
| **Certum 普通个人云签名**  | 可以，但没优势    | 约 $116/年，不卡商业授权 —— 但价钱跟 Azure 打平，而信誉信号不如微软自家的。**没有理由选它。**                                                                                                                                                                                                                                                                                                                                                                                      |
| **Azure Trusted Signing**  | ✅ 可以           | 约 $10/月。**2026 年已改名 Azure Artifact Signing。** 不问许可、不问双授权、不审查软件做什么，只验身份。微软自家服务，是官方推荐的「商店之外分发」签名方式。**但没有即时信誉**（见上面那段纠正），靠连续发布同一签名身份积累。                                                                                                                                                                                                                                                    |
| **继续不签**               | —                 | 每出一版都要向微软重提一次误报申诉（放行按哈希）。                                                                                                                                                                                                                                                                                                                                                                                                                                |

仓库这边**过了**的条件（`gh repo view` 实测）：`PUBLIC`、AGPL-3.0（OSI 认可）、
非 fork、19 star / 5 fork、在维护、GitHub Actions、已有 Releases。**只卡在双授权那一条。**

**走签名这条路之前，三条代价先想清楚（2026-09-22 补）：**

1. ⛔ **签名会把真实法定身份写进每一个二进制**（证书 Subject = 通过验证的姓名或公司）。
   这个仓库现在是以化名 `smithtaylor7748-ops` 发布的 —— 签名等于公开实名，
   而且**不可逆**：已经发出去的签名包永远带着那个名字。这是个产品决定，不是技术决定。
2. 需要 Azure 订阅 + 身份验证（个人档要政府证件；组织档另有成立年限门槛）。
3. ⚠ **签了也不是买断制**：2026-03 微软换了中间 CA（`Microsoft ID Verified CS EOC CA 03`
   一类），一批本来已经有信誉的发布者又开始被 SmartScreen 拦。

### 一条免费的候选（**未验**，2026-09-22 记）

`src-tauri/tauri.conf.json` 现在是 `"installMode": "currentUser"`。
2026-09-22 那次检出的 `Resources` 字段一次点了三样：AppData 里的 exe、
开始菜单 `.lnk`、HKCU 卸载键 —— 这是模型里权重很高的**一组**特征。
改 `perMachine`（或 `both`，让装的人自己选）能把这组拆掉，**一分钱不花**。

代价：安装与升级每次都要过一次 UAC，跟「别人的机器不一定有管理员」那条开源兼容性规矩
冲突。⛔ **改之前先按下面「未验项」的办法量**，别凭感觉 —— 这类启发式的行为没法从代码推出来。

### 0.29.0 的未验项

- 那几项缓解（版本资源、应用清单、去 `-EncodedCommand`、`strip`、开机不再扫桌面）
  **有没有真的降低检出**没有量过。验法：改动前后各把 `*-setup.exe` 交一次 VirusTotal 比检出家数。
- **构建产物的版本资源与内嵌清单要回读核对**，命令见 ANTIVIRUS 第 6 节与 `CLAUDE.md` 收工清单。
  0.29.0 构建后实测：清单六项（`requestedExecutionLevel` / `asInvoker` / `supportedOS` /
  `dpiAwareness` / `activeCodePage` / `longPathAware`）**全部进去了**；版本资源里
  `CompanyName` / `LegalCopyright` / `ProductName` / `FileVersion` 也都填上了。
- ⚠ **`InternalName` 与 `OriginalFilename` 仍然是空的，而且补不了**：`tauri-build` 没有
  设这两项的入口（只设六项，`tauri-build-2.6.3/src/lib.rs:604-670`）。
  ⛔ 别用 `append_rc_content` 去补第二个 `VS_VERSION_INFO` 块，也别构建完改二进制 ——
  两者都让情况更糟，理由见 ANTIVIRUS 第 3 节。

## 0.25.5（2026-09-26）DNS 泄露「JSON 解析失败」· 官方账户页两组修复

DNS 泄露：

- **bash.ws 回对象的那几种形状只在单测里走过。** 作者本机 09-25 真跑一次回的是数组（11 项，认出 9 台解析器）。
  PHP 下标不连续编成的 `{"0":{…},"2":{…}}`、单条记录的对象、`{}` 是照 PHP `json_encode` 的行为推出来的 ——
  **报错那台机器上回的到底是哪一种没见到**（原来的报错只说「是个对象」）。认不出的形状现在报
  「bash.ws 这次回的不是解析器清单（对象：error 字符串、code 数字）」这一类：只列键名与值的类型，**不带值**，
  像 IP 的键名打成 `<IP>`。再有人报，那句括号里的形状就是线索；本机能复现时跑
  `cargo run -p qb-probe --example dns-leak`（只打印形状与条数，不打印任何地址）。
- **代理开着 fake-ip 时 DNS 这一项判不了。** 探针域名被解析成 198.18.x.x，查询被代理接住、没从本机发出去，
  bash.ws 看不到「谁来查的」。报告现在把这件事说清楚，分数仍是「—」、不算通过；代理那一头用哪台解析器，
  只有高级通过（浏览器那一路）看得到。自定义的 fake-ip 段（不在 198.18.0.0/15、fc00::/7 里）认不出，仍是那句「未收到回显」。
- **记录里认不出的字段当「没有」，不丢整条。** `false` / `null` / 空串都当没有，数字当字符串读，国家码不分大小写。
  丢掉的那一条可能正是国内解析器 —— 宁可一条记录缺字段，也不让它从清单里消失。

官方账户页：

- **三种关停现在是三个范围**（DESIGN-NOTES 有一节）：切 Claude 账户只关 Claude 的（桌面端、Claude Code、酒馆桥接）；
  门禁关停不关中转，反重力只在它归门禁时关；「一键关闭」全收。筛法由单测钉着，**真机上没点过**：反重力开着时切 Claude 账户、
  反重力移出门禁后门禁关停，都要看一眼它是不是还开着、切换框报的数对不对。
- **删槽位删不干净时会留一个 `.deleting-<槽位名>-<编号>` 目录**（在 `%LOCALAPPDATA%\ClaudeIpGate` 下，跟槽位目录同级）。
  它已经不是槽位：列表里没有、切不过去。面板不自动清它，删除结果里写着路径，关掉占着文件的程序后手动删。
  「完全卸载」连整个目录一起删，不受影响。
- **GPT 槽位与 Gemini CLI 那一半的「最近一次」额度按槽位存，没记是谁问的。** 同一个槽位里退出、换登另一个账户，
  点刷新之前格子里是上一个账户的数。反重力 IDE / Hub 那两处记了邮箱、对不上就不拿出来；Gemini CLI 做不到同样的事 ——
  面板只看它的凭据文件在不在、不读内容（0.32.0 那一节「槽位迁移不猜配对」是同一个理由）。
- **「窗口已重置」只在知道重置时刻时才判得出。** Claude 那边的重置时刻来自 Claude Code 留下的缓存，或者从桌面端样本的
  断崖推出来（只推五小时那一格）；GPT 看快照自己带的 `resets_at`。都没有的那一格（比如只有桌面端样本时的七天窗口）
  仍只按年龄判「读数已过期」—— 在那之前照旧画旧数。
- **这一轮的界面改动只在演示模式与 `test:ui` 里走过**，真机上没点过：切换框的新扫描范围与文案、一键关闭确认框、
  反重力「并入… / 取消」、没有 IDE 那一半时「建并登录」现建目录、启动卡里的「修复 Codex 注册」、
  「设为中文」只关只重开官方槽位（中转环境的 Codex 这次不动；激活槽位原来没开着就不替你开）。清单在桌面 `claude-op` 的 `20`。

## 0.25.4（2026-09-25）中转站倍率按「中转站百科」帖改对 · 各站的充值比例

- **New API 的分组倍率与充值价只在单测和演示里走过**，没对着真实站点核过。字段形状照 New API 源码
  （`controller/pricing.go` 的顶层 `group_ratio`、`controller/misc.go` 的 `price` / `usd_exchange_rate`），
  改了形状的分叉版本会读不到 —— 读不到就是「不知道」：分组倍率不知道的线路不参加加权比价，充值价按钮不出现。
- **New API 倍率换单价用的是它写死的 $2 / 百万**（`1 === $0.002 / 1K tokens`，它自己的定价页也写死 2）。
  严格说是 `1_000_000 ÷ quota_per_unit`；改过 `quota_per_unit` 的站，它自己页面上的单价跟账单就对不上，
  面板显示的单价跟着错。**账单核对不受影响**（那边是 `quota ÷ quota_per_unit`）。
- **分组名留空按 New API 的默认分组 `default` 查倍率。** 你的账号要是被站长划进了别的分组、令牌又没指定分组，
  实际走的是那个分组 —— 这时填上真实分组名才对得上。`auto` 分组的倍率跟着实际路由走，不是一个数，不认。
- **分组倍率不知道的线路不参加「加权倍率」那种比价**：按不打折算出来的是牌价，跟别家的折后价放在一起比等于冤枉它。
  池子里有一条这样的线，整池就退回「真实倍率」（标称 × 实测）那个粗口径 —— 是有意的。
- **「输出加价」只在检验时算**（要官方价，而官方价按「这组价是哪个模型的」查）：老线路、官方价快照里没有的模型
  （比如 `gpt-5`）都不显示，**不等于「没加价」**。0.25.3 及以前界面上的「翻倍 ×5」读的是 `completion_ratio > 1`，是误报。
- **充值比例要自己填。** New API 站点在 `/api/status` 公布的在线充值价只做成一个参考按钮，**不自动采用**：
  那一格默认值就是 7.3，很多站没改过、实际靠兑换码按 1 元卖；照它折算会把这些站冤枉贵七倍。sub2api 没有这一项。
  它是站点级的（整站共用），只用于跨站比较（调度的「便宜」与「倍率不超过」）和线路上那句「折合」；
  查套路的账单核对照旧按站内数值比，余额也照旧按站内额度写。
- **0.25.3 及以前存下的检验历史**：New API 那几类当时只存了原始倍率、没乘分组倍率，现在按 $2 / 百万换成单价显示 ——
  是站点**不打折的牌价**，不是那条线实际的价。重新查一次套路就是新口径。

## 0.25.4（2026-09-25）一键汉化：Claude 插件 claude-desktop-zh-cn · GPT 官方语言设置

- **Claude 那条没在真机上点过。** 核到的只有「只查、只下、只核」（`cargo run -p qb-app --example claude-zh -- check`
  对着上游 1.4.8 跑通：标签、提交号、MIT、脚本参数、挑出的 19 个文件）与演示模式里的界面流程。
  没验的：上游 1.4.8 的安全模式在 Claude 2.9939.2 上真跑一遍、它自己重启的那份 Claude 被面板按证据关掉、
  改前改后的哈希与签名比对、别的账户资料的 `locale` 写上又写回、恢复英文从 `.zh-cn-backups` 还原干净。
  ⚠ 要在**不在 Claude 桌面端里干活**的时候点（它会关掉 Claude，包括 Code 页里正在跑的会话）。
- **非官方修改，Claude 一更新就没了。** Squirrel 更新会换一个新的 `app-<版本>` 目录，汉化随之消失；按钮会变成
  「汉化：需重新应用」（使用者选的：点一下补，不改启动流程）。上游还没跟上新版 Claude 时，它的脚本会报
  「前端 bundle 格式可能已经变化」—— 面板照原话显示，并用它自己的卸载撤回改了一半的部分。
- **在线的 claude.ai 页面（对话、项目等）仍是英文。** 那部分上游要改 `app.asar`（official 模式），面板永远不调用。
  安全模式覆盖的是 Claude 桌面端本地的界面（设置、侧栏、Code / Cowork 那一侧、菜单等）。
- **只支持官网安装包（Squirrel）装的 Claude。** MSIX 版装在 WindowsApps 里，上游要以管理员接管那个目录的 ACL，面板不做。
- **中转环境的桌面端不跟着改**（它们用 `CLAUDE_USER_DATA_DIR` 指向各自的资料目录）；GPT 的中转环境同样不改。
- **上游脚本最后会自己用 `explorer.exe` 起一次 Claude**（没有开关能关掉它）。面板等它出现（最多 20 秒）再按证据关掉；
  它起得比 20 秒还慢的话，会留下一份不归面板管的 Claude —— 出口 IP 在那之前刚验过，但它没有租约、看门狗不认它。
- **组策略强制 `AllSigned` 的机器上跑不了**：面板用 `-ExecutionPolicy RemoteSigned` 起上游脚本，机器级策略会盖过它，
  脚本报错、面板照原话显示。
- **杀软可能对「下载脚本再执行」起疑。** 面板没签名、零信誉，这一条是新的行为信号（见上面「代码签名 / 杀软误报」）；
  已经尽量减轻：不提权、不 base64、用 `RemoteSigned` 而不是 `Bypass`、上游的 Frida 脚本一个字节都不落盘。
- **Anthropic 看不看得到你的界面语言，面板没核实。** 安全模式不改桌面端的请求头；但桌面端本来就会上报系统的首选语言
  （2.9939.2 的 `app.asar` 字符串里有 `preferredSystemLanguages`，没抓包），汉化与否它都看得到。
- **GPT：设了中文界面仍是英文**是 OpenAI 的远端开关 `enable_i18n` 没给这个账户 / 机器开（openai/codex#19239、#23815、
  #24827）。面板只写设置项、不碰那个开关；「GPT 上次运行时用的是 zh-CN」说明它读到了设置，不说明界面翻译了。
  还有一种是后台进程没退干净、重开复用了旧状态（openai/codex#16540）：面板起的那份按槽位关干净再重开，别处起的那份不管。
- **GPT：别处起的那份开着时默认那一份（`~\.codex` 或 `CODEX_HOME`）这次不改**，关掉它再点一次。
- **GPT：界面语言很可能随请求带给 OpenAI**（面板没核实）；体检里「浏览器语言与出口地区对不上」跟它是同一类信号。

## 0.25.3（2026-09-24）打开面板弹窗提醒新版本 · 一键更新

- **端到端还没真跑过。** 0.25.3 是第一个带更新功能的版本，要等下一版发到 GitHub 上，装着 0.25.3 的机器才第一次
  真正走完「弹窗 → 下载 → 核对 SHA-256 → 面板退出 → 安装包 `/P /UPDATE /R` → 重开」。单测只盖到纯函数（版本比较、
  `update.json` 与 `SHA256SUMS.txt` 的解析）与退出处理里的先后顺序；弹窗在演示模式里由 `test:ui` 走过一遍，但演示不下载、不安装。
  下一版发出来之后要在真机上装着 0.25.3 点一次，看：安装包是不是被动模式（只有进度条）、装完面板有没有自己重开、
  桌面快捷方式与开机自启项有没有被动过、`%LOCALAPPDATA%\ClaudeIpGate\updates` 在下次启动时有没有被清掉。
- **v0.24.8 及更早版本收不到提醒**（里面没有这个功能），要手动下载安装一次。README 顶上、Release 正文都写了。
- **只提醒比自己大的版本号。** 装着 0.26–0.32（只在作者本机出现过的号）的机器收不到 0.25.x 的提醒；以后发一个比已经发出去的
  更小的号，所有人都收不到。
- **完整性只到「跟同一个 Release 里的 `SHA256SUMS.txt` 一致」**，跟 README 教大家手动核对的是同一件事：挡得住下载损坏、
  被中间人换包，挡不住有人拿到仓库发布权限之后连安装包带哈希一起换。安装包本身没有代码签名（见上面「代码签名」一节）。
  要更强就得换成 Tauri updater 的签名（使用者 09-24 选了不做：要保管私钥、丢了就断更）。
- **更新时面板会退出**，它启动的桌面端、对话、酒馆跟着 Job 一起结束（坑 7.38）。弹窗按钮上方常驻写着这句；
  面板没有「等你把对话存完再装」的办法。
- **启动检查查不到 GitHub 时不打扰人**（没有弹窗、没有提示），原因只在设置页「软件更新」那一行和审计日志里。
  所在网络连不上 `github.com` 的人会一直收不到提醒。
- **下载的安装包放在 `%LOCALAPPDATA%\ClaudeIpGate\updates`**，面板下次启动时删；安装包还在收尾时删不掉就再等一次。

## 0.25.2（2026-09-24）用量的美元 · 订阅页倍率 1:7 · CheckClaude 三组 · DNS 评分

用量：

- **快速模式（`usage.speed` 不是 `standard`）没有官方价。** 这种回复的模型名带 ` (fast)` 后缀、进「没有官方价」那一栏，
  不拿标准价凑。官方给快速模式单独标价之后要把价补进价目表。
- **GPT 的美元在有长上下文请求时只是下限。** 单次输入超过 272K 的请求官方按更高一档算输入价，会话记录里看不出
  每一段差分落在哪一档，所以只计数、美元写「≥」，不猜档位。
- **GPT（Codex）一侧的按天、按模型与美元没在真机上验过。** 本机 `~\.codex\sessions` 是联结点、按规矩整棵拒读
  （见 0.32.0 那一节），只有单测喂的形状。
- **「今天 / 近 7 天 / 近 30 天」三格不随选中的档位变**，是故意的：三个数要同时看见。
- **账户页用量卡仍走单槽位的轻量路径**（不带按账户、最近请求与价目明细），这些只在 `/usage`。
- **最近请求只列 200 条**（扫描时每个槽位封顶 500），按时刻倒序；更早的只进合计。

订阅页：

- **倍率按 1 美元 = 7 元折算，不跟实时汇率走**（使用者定的，往贵了取）。实际汇率一般在 6.8 左右，按它算倍率再低约 3%。
- **比较的前提是中转站按「1 元 = 1 美元额度」充值**（常见口径，页面上写着）。按真实汇率充值（约 7 元 = 1 美元额度）的站，
  它标的倍率要乘 7 才能跟页面上的数比；其他比例照此换算。腾讯云那篇原文只说「倍率 1 = 同价」，没说充值比例。
  （0.25.4 起中转站页每个站点能填这个比例，调度按它折算；订阅页的计算器里「付的钱 ÷ 中转站给的额度」那一项本身就是它。）
- **计算器里「中转站给你的额度」是站内余额**，按那条线路的分组倍率扣 —— 0.25.4 起旁边多一格「那条线路的倍率」（默认 1）。
  包月会员那种按牌价扣额度的，留空就对。
- 订阅页那几个周额度与倍率区间都是网友 / 文章的估算，见 0.25.1 那一节；换算方式改了，估算本身没变。

环境监测：

- **API 可达量的是「绕过代理」那一路**（跟门禁同一个客户端）。只开系统代理、没开 TUN 时，直连那一路回 403 会判失败、
  评分封顶，即使浏览器与桌面端走的系统代理那一路是通的 —— 说明里会写出两路各是什么。理由：Claude Code（Node）
  不认 Windows 的系统代理，门禁量的也是这一路。
- **claude.ai 回 403 不一定是地区拦截。** 带 `cf-mitigated` 的是 Cloudflare 人机验证页，算「查不出」；不带的只降到警告。
  判定以 API 那一个为准。
- **自定义 fake-ip 段会被报成「可疑」**（警告）。只认得 198.18.0.0/15 与 fc00::/7；别的私有段按「被污染」、
  别的公网段按「可疑」。
- **IPv6 出口只查到国家。** 回显服务三个都连不上就当「没有 v6 出口」—— 那三个服务自己挂了时会误报成通过。
- **真实浏览器采集只测打开那个链接的浏览器**，结果一小时内有效、只在内存里（面板重启就没了）。
  **没在装好的面板里点过**：开发模式与单测验过服务端（令牌、Host、只收一份、超大、超时）与演示界面；
  正式包里页面由 Tauri 的内嵌资源供给，那一条要实机点一次（写在桌面 `claude-op` 的实机清单里）。
- **代理形态按网卡的 `HardwareInterface` 认 TUN**，Hyper-V 的虚拟交换机排除。读不出路由表或网卡（PowerShell 受限）时是「查不出」。
- ~~DNS 泄露评分里「以太网安全」那 50 分按网卡名认，只连 Wi-Fi 的机器最高 50 分~~ —— 同一天改掉了，见下面几条。

DNS 泄露（2026-09-24 评分改过）：

- **网卡那一项只在 TUN 开着时算。** 没开 TUN 就没有隧道可绕，路由器的 DNS 是正常的；这时 DNS 这一项只按真实解析
  （bash.ws 回显）算。「TUN 开没开」问的是发往一个 RFC 5737 文档地址（`203.0.113.1`）的路由走不走非硬件网卡 ——
  开着 Tailscale 出口节点、WireGuard 全局这类也算「开着」，这是对的：那时同样有隧道可绕。
- **「走不走隧道」靠 `Find-NetRoute`。** 查不出路由（没有这条命令、路由选中回环这类 `Get-NetAdapter` 不认的接口，
  比如 DNS 配成 `127.0.0.1` 的本机 DNS 代理）的那一条不判、不计分。只看 IPv4 的 DNS，网卡上配的 IPv6 DNS 不看
  （之前就是这样）。
- **Windows 会同时向每张网卡的 DNS 发查询**，所以「连着的物理网卡上配着不走隧道的 DNS」才算缺口；隧道软件拦不拦得住，
  最终以回显为准（占 70 分）。
- **存盘的旧 DNS 报告不进总分，要重测一次。** DNS 结果会留到下次开面板；升级后读出来的旧报告按旧规则算过分，
  格子上写「评分规则改过，请重测一次」。

## 0.25.1（2026-09-23）联网额度只手动刷新 · 反重力四格与 AI 积分 · 令牌在内存里换新

- **订阅页的周额度是网友估算，而且会过时**（2026-09-24 换成 linux.do 帖子的中间值）。帖子作者自己写着
  「不一定准」；Claude 那几档是 2026 年 8 月官方临时 1.5 倍额度时测的，活动一结束就偏高了；
  「一个月 = 4 个周限」是本页取的整数，不是官方口径。帖子更新了要手动跟着改（`data.ts` 的区间与中间值一起改，
  单测会盯着「中间值 = 区间中点」）。
- **反重力联网额度那几个接口没有公开文档，这一轮也还没拿真实账户跑过。** 字段名与
  `bucketId`（`gemini-5h` / `gemini-weekly` / `3p-5h` / `3p-weekly`）是读 cockpit-tools 的源码与它在
  使用者本机留下的缓存形状核对的，解析器是自己写的、单测喂的是照那个形状编的报文（**单测不联网**）。
  换机器、上游改版、界面开始说「读不出来」时先跑
  `cargo run -p qb-app --example antigravity-hub-quota`（加 `-- --account <槽位 id>` 问账户槽位；
  会用你自己的令牌联网一次，只打印键名、类型、长度）。
- **令牌换新依赖本机装的反重力。** 换新要带签发令牌的那个客户端的标识，面板不内置，点刷新时从
  IDE 的 `resources\app\out\main.js`（读不出再扫语言服务器，一百多 MB，慢一点）里现读、按位置配对、
  不对就换下一组（最多 6 组）。只装了 Hub、或者反重力某次更新换了存法 → 换不下来，界面说
  「起一次这个账户的反重力，让它自己换新，再点刷新」。IDE 2.5.5 上核过里面有两组，**哪一组对应
  哪个令牌没验**（要真去换一次才知道）。
- **换新这件事本身是在用反重力客户端的身份跟 Google 说话。** 使用者 2026-09-23 拍板做的；
  Google 对「第三方程序拿官方客户端的刷新令牌换访问令牌」有没有意见，本项目不知道，DISCLAIMER §6 写了。
  请求形状照官方 IDE（`User-Agent: antigravity/<本机 IDE 版本> windows/<arch>`）也是同一件事的一部分。
- **一格用光时 Google 大概不给 `remainingFraction`**（proto3 的 JSON 省掉零值），这时按 0 算、
  悬停里说出来 —— **推的，未实测**（没拿用光的账户问过）。
- **免费档没有四格。** Google 对免费档的汇总回 403（`SUBSCRIPTION_REQUIRED`），账户行改成按模型的两组
  （Gemini / Claude·GPT，各取最低）。免费档的「按模型」比例是不是每周那一份，没核。
- **没有 project 的账户不发 `onboardUser`。** cockpit-tools 会发（那是登记档位、改账户状态的调用），
  这里只发 `{}`。付费账户这样答得上来；从没用过反重力、还没被登记过的账户会不会因此问不到，没核。
- **「最近一次」只在内存里。** 面板重启就没了，要再点一次刷新；没有 TTL，界面标明是几点问的。
- **Claude 的刷新图标不联网。** 它只重读本机记录 —— Claude 额度只读本机这条没变。
- **窄窗口（容器宽 ≤850px）下反重力账户行的额度区整块收掉**（连同刷新图标），跟原来那一根条一样；
  额度在右下用量卡「剩余配额」一格和 `/usage` 上仍有。
- **GPT / Gemini 登录态那一轮（2026-09-23）没在真机上跑过联网那两条。** Gemini CLI 的换新要使用者本人的
  Google 账户；只核过单测（令牌形状、客户端标识扫描、回复解析）与演示界面。几件没核的：
  - Gemini CLI 包里的客户端标识在哪个文件、跟哪一组配对 —— 按「入口脚本 + 同目录 `.js`」去扫，
    **没对着真实安装跑过**；包换了打包方式（比如标识被拆到别的目录）就会报「本机没找到 Gemini CLI 的
    客户端标识」，这时退回到「在 CLI 里随便跑一句让它自己换新，再点刷新」；
  - GPT 那条假定 Codex 的访问令牌是带 `exp` 的 JWT；读不出 `exp` 时照旧发请求，由服务端说话；
  - 「登录已失效」只在内存里，面板重启就忘；Hub 那一条没有账户行，不记。
- **GPT 的访问令牌到点之后，额度刷新要先开一次那个槽位的桌面端。** 面板故意不替它换新（OpenAI 的刷新令牌
  会轮换，换了桌面端就被登出）。
- **起 / 切 GPT 槽位之前只关面板起的 Codex 实例。** 读不出命令行的实例（比如以管理员跑的那份）一律当成别人的、
  不关；如果那恰好是面板上一次起的同一个槽位，新起的那份会被 Electron 转交给它，面板这边报「客户端启动后立即
  退出」—— 从任务栏退掉它再点一次。开始菜单起的默认实例现在会跟槽位的窗口同时开着，这是故意的。
- **快速跳转里的 GPT / 反重力账户是搜索框打开那一刻读的。** 开着搜索框的时候新建或移除的槽位，
  要关掉再打开才反映出来（Claude 的跟着账户页的轮询走，没有这个问题）。点搜到的账户只带你去它那一边，
  不切换激活槽位 —— 这是故意的，理由见 DESIGN-NOTES。
- **菜单栏那五处修复只在演示界面与 `test:ui` 里验过**，真机上没点过。改动全在前端、不碰数据来源，
  真机上的差别只会是数据本身（槽位名、有几条）。

## 0.32.0 账户槽位合并 · 额度条 · 用量明细页 · 酒馆控制面板

- ~~反重力 Hub 的配额解析器没在真机上验过 / Hub 的访问令牌一小时过期、面板不会替你换~~ ——
  2026-09-23 那个解析器整个换掉了，令牌也改成过期时在内存里换新，见上一节。
- **GPT 的额度是上一次请求时的快照，不是此刻。** 读的是 Codex 自己写在会话记录里的
  `rate_limits`。Codex 关着的这几个小时里额度早就在恢复，面板不知道 —— 界面标着
  「Codex 写入 hh:mm」。**走中转或 API Key 的会话不带额度**（整块是 `null`），
  那种槽位可能一条带数的记录都没有，界面说「还没有带额度信息的会话记录」，不是 0%。
- **`~\.codex\sessions` 如果是联结点，用量与额度都读不出来。** 本机实测就是这样
  （多半是别的工具建的）。`config_io::ensure_plain_path` 按规矩拒掉整棵树 ——
  这是有意的（§7.18 悬空联结点），但代价是那台机器上 GPT 的两项统计都空。
  诊断例子现在会把**第一条失败的原因**打出来（原来只有一个计数，查不出所以然）。
- **槽位迁移不猜配对。** 老的 IDE 槽位与 Gemini CLI 槽位各自升成一条「只填了一半」的行。
  面板没有任何依据断定两者是同一个 Google 账户（IDE 的邮箱在状态库里，CLI 那边只看
  凭据文件在不在、从不读内容）。要合成一条得自己点「并入…」，那一步也只改索引、
  **不搬也不删任何目录**。并错了再建一条指回去。
- **迁移只在面板启动时跑一次**（`sync_ide_profile` 里，幂等）。它失败不让面板起不来，
  只写一条审计 —— 老索引一个字节没动，下次启动还会再试。
- **窄窗口（≤780px）下账户行只剩名字与两枚徽标。** 配额条与那句状态收掉了 ——
  `.account-slot-list` 是四行固定高度网格，第三层装不下。配额在右下用量卡和 `/usage`
  上都有，收掉不丢信息；但「登没登录」这一位不收。
- **Claude 桥的设置与监控要那份 `bridge.py` 有对应接口。** 它是使用者自己那份、
  **面板不分发**；别人机器上可能是别的版本甚至没有。连不上 / 404 时界面如实说，
  并给出下一步（起酒馆 / 升级 / 重起一次让密钥对上）。内置的模型与 effort 选项是按
  2.4.0 抄的快照，**桥当前用着的值永远算合法选项** —— 但快照本身会过期。
- ~~`/usage` 的趋势图只有 Claude 与反重力两侧有，Codex 没有按天、按模型的分项~~ ——
  2026-09-24 起 GPT 一侧也有了（差分记到样本时刻的本地日、记到最近一条 `turn_context` 的模型上），
  但没在真机上验过，见「0.25.2（2026-09-24）」那一节。
- **这一轮的真机验证只做到「解析器对得上本机数据」这一步。** 已验：Codex 的
  `rate_limits` 解析器（拿本机真实 rollout 跑通，读出 7 天窗口已用 3%）、
  Hub 凭据的形状与 `id_token` 里的邮箱、Gemini CLI 位置表。**没验**：Hub 配额那次真实响应、
  槽位迁移在真有两套老槽位的机器上、`bridge.py` 的四个接口端到端、
  `/usage` 在装好的面板里（浏览器演示模式证明不了任何事，§7.37）。

## 0.31.0 Gemini CLI 定位 · 酒馆就绪 · 三条桥的设置

- **Gemini CLI 的定位修好了，但只验到「检测认得出」这一步。** 本机实测
  `cargo run -p qb-install --example gemini-resolve` → `installed = true` / 0.60.0 /
  入口 `…\bundle\gemini.js`（0.26.0–0.30.0 找的是不存在的 `dist\index.js`，于是 npm 每次真装成功、
  面板每次报「还是装不上」）。**没验的**：软件页那颗「用 npm 安装」在**还没装过**的机器上跑一遍；
  npm 前缀不是默认值的机器（`NPM_CONFIG_PREFIX` / nvm-windows 的 `%ProgramFiles%\nodejs`）上
  认不认得出 —— 那两条候选目前只在代码与单测里成立。
- **酒馆的 Gemini 桥接端到端仍未走过。** 定位修好之前它从 0.26.0 起就没起来过（同一张位置表）。
  现在还差本机没有任何 Gemini 槽位这一步 —— 要使用者本人的 Google 账户：新建槽位 → 登录 →
  起桥接 → 酒馆里发一句话收到回复。
- **酒馆就绪窗口 180 秒是按本机量的。** 2026-09-21 实测：依赖全热时 47.2 秒。
  **更慢的机器仍可能超**（冷启动要联网装依赖、杀软扫 449 个包）。超了不再杀进程、
  只是不再等 —— 界面说清楚了，但「等多久算够」没有一个对所有机器都成立的数。
- **`.no_proxy()` 那条只在本机确认「没撞上」，没在真有 `HTTP_PROXY` 的环境里验过。**
  本机当时 `ProxyEnable=0`、也没有环境变量。
- **桥接设置弹窗里 Claude 那条的状态是「见插件页」，不是实时的。** 它活在面板进程外
  （使用者自己的 `bridge.py`），面板这一层只知道端口占没占 —— 不谎报，也就没显示。
- **酒馆那边的扩展改的是使用者本机那一份**（`claude-tavern-bridge` 1.2.0，
  原文件留了 `.before-multi-bridge-<时间戳>.bak`）。**面板不分发它**，所以这个修复不随安装包走；
  别人机器上那份仍是写死 5001 的老版本。三条桥的探活在本机真酒馆里验过
  （起一个占 5002 的监听 → 列表正确显示「GPT 桥接 在跑」、Claude 那条仍是「没在跑」
  且「独立打开」按钮置灰）。

## 0.30.0 反重力账户页：IDE 槽位 · 配额 · 用量

- **IDE 槽位在本机走通了探针，别的机器没验。** 探针（2026-09-21）：临时目录起 IDE → 它要求重新登录
  → 登第二个 Google 账户 → 两份资料目录各持各的邮箱与档位（一份 Google AI Pro、一份免费档），
  同时跑互不影响。**没验的**：同一台机器上两个槽位轮流起、语言服务器共用的
  `~\.gemini\antigravity-ide` 会不会串状态（对话记录、`installation_id`）；IDE 升级后
  `--user-data-dir` 还认不认（VS Code 的标准开关，大概率认）；装了真正的第三方壳（本机那份
  「壳」跟本体字节相同，不算）的机器上参数传不传得进去。
- **新槽位是一份干净的 IDE。** 只复制默认资料的 `settings.json` / `keybindings.json`；主题、
  扩展启用状态、工作区历史、MCP 配置（`~\.gemini\antigravity-ide\mcp_config.json` 是共用的，
  不受影响）等从头来。这是目录隔离的代价，不是 bug。
- **只有从面板起的 IDE 用槽位。** 开始菜单起的仍是默认资料目录那份。
- **配额只有 IDE 上次同步时那么新。** 它读的是 IDE 写在 `state.vscdb` 里的 `userStatus`，
  IDE 关着就停在最后一次；界面写「IDE 写入 hh:mm」。**Hub 不写这份状态** —— 只装 Hub 的机器上
  配额与邮箱一片空白，界面说「登录后可见」。字段号是实机解出来的（没有 Google 的 schema），
  上游改了形状会读不出 —— 那时显示「读不出来」，不会显示错数。
- **用量字段含义按实测推断**（12,410 条记录：1 系统提示 / 2 输入 / 3 输出 / 5 缓存读），
  跟 Antigravity-Tools-Lite 的读法一致，但都没有官方 schema。**旧 `.pb` 归档不计入**
  （本机 16 份，解不出模型名），界面报数。备份目录（`antigravity-backup`）里同名对话只计一次。
  没有账户标识，**不按账户摊派**。`unknown`（没写模型 id 的记录）单独一行。
- **Gemini 价目是促销价。** 3.6 / 3.7 / 3.8 Flash 官方页写「$0.75 至 2026-12-31，2027-01-01 起 $1.50」，
  内置快照按 2026-09-21 的页面抄的，`verified_at` 到期前要重核；面板启动时抓的官方价只有
  Anthropic / OpenAI 两家，Google 的页面没有机器可读的版本，**Gemini 一直走内置快照**。
  「缓存省下」按官方 API 价估算，不是订阅实付。
- **配额只显示。** 没有任何「额度低了换槽位」的路径 —— 合规边界头两条。

## 0.29.0 反重力一键安装与 Gemini CLI（真机待验）

**反重力一键安装**（`install::antigravity_setup`）核到「拿到版本与下载地址」为止
（`cargo run -p qb-install --example antigravity-resolve` 实跑通过：Hub 2.15.1、IDE 2.5.5、
地址都在 Google 自己的域上，本机两个产品都已装）。**没验的**：

- A 路 winget 真装一遍（`Google.Antigravity` / `Google.AntigravityIDE`）；
- B 路真下载一遍、`Get-AuthenticodeSignature` 读得出 Google、静默开关真的静默
  （尤其 IDE 那串 Inno 开关里的 `/mergetasks=!runcode` —— 少了它安装器会把 IDE 拉起来，
  而面板紧接着要上锁）；
- 装完 `detect::antigravity()` 认不认得出；
- **装完 Deny ACE 有没有真的重新加上**（`Maintenance::observing` → `finish` 那一段）；
- ARM64 机器上挑的是不是 `windows-arm`（Hub）/ `windows-arm64`（IDE）—— 两个产品的
  ARM 目录名不一样，本机是 x64，这一条只在代码与单测里成立。

**Gemini CLI 安装**根因已实机复现并修好（引号吃掉 `/k`），但**修复本身没在真机上点过**：

- 有 Node 的机器上装一遍，看进度条与回读核对；
- **没有 Node 的机器上**给的是不是「没找到 Node.js」那句话，而不是一个空窗口。

> ⚠ 0.31.0 补：上面那条「修好了」只对**命令**成立。回读检测用的**位置表**当时还是错的
> （找 `dist\index.js`，而官方包的入口是 `bundle\gemini.js`），所以 0.29.0 / 0.30.0 上
> npm 每次都真的装成功、面板每次都报失败。**根因修了一半不等于修好了** ——
> 判据链上每一环都要各自验一次。见上面 0.31.0 那一节。

## 0.22.0 Codex 桌面账户与检测范围

- 官方账户页固定为等宽、等高两栏，最低窗口 680×640；账户四个一页，详细说明移入弹窗。其他内容页和详情弹窗仍可按需滚动。
- 当前识别 Microsoft Store 的 `OpenAI.Codex_2p2nqsd0c76g0` 包，支持包内 ChatGPT.exe / Codex.exe；独立安装路径暂未接入。
- 新槽位在官方桌面端完成 ChatGPT 登录；不导入默认目录的 API Key，不复制 OAuth refresh token，不连接内部配额接口。
- 切换先关闭 Codex 桌面端及其子任务，确认框明确提示中断；若 Codex 是 QB Gate 的祖先进程则拒绝关闭，需独立打开 QB Gate。验证新版本时不在正在工作的 Codex 上执行关停。
- 独立槽位用量只统计该目录的本机会话，不认领默认目录历史；累计计数重置、缺少会话 ID/时间或坏文件会报告统计不完整。不是订阅剩余额度或实际账单。
- ~~Codex 桌面端不继承 Codex CLI 的文件执行锁~~ 0.25.0 起 GPT（Codex）**默认归门禁管**：账户页起桌面端走 Claude 页同一条链（验 IP → 托管会话 → 租约 → 看门狗 Desktop 档），`codex.exe` 一并上执行锁；在「IP 锁」弹窗里可以把 GPT 移出。移出后才是 0.22–0.24 那种独立启动。
- 一键修复覆盖有证据且可安全处理的 WebRTC/DoH 用户策略及无租约时未锁的已发现副本。IP 信誉、系统 DNS/代理选择、语言字体不自动修改。
- 浏览器策略写入与真实流量行为是两类证据，仍需重启 Chrome 并核对 chrome://policy；DoH automatic 允许退回系统 DNS。

## 0.28.0 Codex 桌面端直装 · 软件页卸载补全

- **直装那条链只在真机上核到「拿到下载地址」为止，没有真的下载安装。** 2026-09-20 用
  `cargo run -p qb-install --example codex-store-resolve -- locate` 核过：DisplayCatalog → FE3
  `GetCookie` / `SyncUpdates` / `GetExtendedUpdateInfo2` 三步都通，读到 Store 最新版
  `26.915.4065.0`、清单 SHA-256 与 825 MB 大小、`tlu.dl.delivery.mp.microsoft.com` 的地址，
  `HEAD` 回 200 且 `Content-Length` 跟清单一致。**没验的**：825 MB 真下一遍、`Get-AuthenticodeSignature`
  读 `.msix` 的主体、`Add-AppxPackage` 在普通权限下是不是一定撞 `0x80073D28`（按 §7.42 的经验很可能）、
  UAC 那条路、装完 `codex_desktop::detect()` 认不认得出。本机装的还是 Store 自己装的那份。
- **A 路 `winget install --source msstore` 在个人 Microsoft 账户下能不能走通，没验。** 已知 `winget download`
  从 msstore 源要 Entra 登录；`winget install` 按文档不用。装不上时会如实报 winget 的输出，然后自动走 B 路。
- **FE3 那套是 Windows Update 客户端的协议，微软没有公开文档。** 信封模板与匿名票据是 StoreLib 系工具
  多年共用的；微软哪天改了，症状是「查 Store 版本」报错或直连拿不到地址 —— A 路和 Store 本身不受影响。
  改版时先跑 `codex-store-resolve -- dump` 看报文再改解析器。
- **下载走的是 `http://`（FE3 给的原样地址）。** 分发 CDN 不给这个主机名配证书（实测 `https` 是 Fastly 的
  兜底证书、主体对不上），Windows Update 自己也是明文下靠哈希保完整。这里同样：SHA-256 来自 TLS 上拿到的清单，
  装之前还核 Authenticode。清单里没给 SHA-256 的版本只剩签名一道闸，日志里会写明。
- **反重力的凭据管理器条目只按名字含 `antigravity` 认。** 语言服务器 `CredWrite` 用的目标名没有核过；
  名字不含它的条目面板认不出，「完全卸载」清完请自己到凭据管理器核一眼。反重力卸载**整目录删**、不逐个核 exe 签名
  （IDE 壳可能是第三方汉化工具签的）—— 归属依据是位置表，不是签名。
- **卸 Codex 桌面端不动账户槽位的 `home`**（登录身份）：Codex CLI 也在用它。桌面端和 CLI 都卸掉之后槽位会留着，
  去账户页「移除」。**卸 Gemini CLI 不动 `~\.gemini` 本身**（反重力的数据也在那底下），只删槽位、npm 包与托管那份。
- **软件页的托管安装（Claude Code / Codex CLI）0.22.6 到 0.27.0 从来没跑通过**（`managed_install` 在调用方已拿着的
  独占锁上再拿一次，永远等着）。0.28.0 去掉了里面那次；真机上装一遍仍未验，见 claude-op 待办。

## 0.26.0 反重力（Google Antigravity）

- **没有中转路径。** Hub 把 API 端点写死在自己的 `app.asar` 里、只认 Google 登录；中转站页不会列它，
  建环境 / 走本机路由 / 存中转启动方案一律报「反重力没有中转路径」。要改只能改 Google 的程序或做
  中间人，两条都在本项目边界外。「本机路由加 Gemini 协议线路」另开一轮，供酒馆等客户端用。
- **Hub 没有多槽位**（IDE 0.30.0 起有，见上面那一节）。Hub 的令牌在 Windows 凭据管理器里，
  目录隔离隔不开；换号在它自己界面里做。面板显示的 Hub「登录态」只是语言服务器日志里有没有
  「Auth succeeded」这一行 —— 版本换了措辞就读不到，界面上写的是「未见登录记录」而不是「未登录」。
- **免 TUN 代理没有做**（使用者定的）。需要的人自行使用 EasyAntigravity，风险自负。
- **自动更新的缺口。** electron-updater 在 Hub 退出时静默装新版本，新 exe 到下一次上锁之前没有 Deny；
  账户页可关掉它的「自动检查更新」（只并入它自己的一个设置键，重启 Hub 生效）。IDE 同理。
- **IDE 的第三方汉化壳。** 本机若装了别的汉化工具（`Antigravity IDE.exe` 是壳、
  `Antigravity IDE.original.exe` 是本体），两份都锁、都收；面板起的是壳，汉化跟着走。
  ⚠ 0.27.0 起面板自己的汉化引擎也管 IDE，而 **IDE 的调试端口是面板启动时补上的**
  （`--remote-debugging-port=0`）—— 那层壳有可能不把这个参数传给里层，于是端口文件不出现、
  引擎附不上去。界面上会如实说这一句，不是静默失败。两套引擎同时跑不冲突
  （已经译过的文字匹配不上字典，不会被二次替换），但没必要。
- **IDE 只有面板起的那一份能汉化。** 使用者自己从开始菜单起的 IDE 没有调试端口
  （VS Code 分支默认不开），引擎附不上去。Hub 不受这条限制 —— 它自己的 `main.js` 总会补端口。
- **汉化引擎依赖 Hub / IDE 的界面结构。** 字典按界面文字逐字匹配，改版后对不上的那些会显示英文；
  审批卡靠 `data-testid="interaction-continue-button"` 等选择器，改版后自动审批可能失效
  （症状：审批卡一直停着）。字典可在状态目录放 `dict.override.json` 追加。
  ⚠ 引擎报的是**回读核实过**的页面数（0.27.0 起注入之后会读回 `window.__ea_engine_running`），
  所以「已在 N 个页面核实生效」是真的；显示 0 就是真的没生效，去看引擎日志。
- **自动审批 = 替你放行。** 只拦规则里写了的命令（出厂九条），其余一律点掉；请自行评估后再开。
- **Gemini CLI 是 `node` 跑的脚本**，跟 npm 装的 `codex.cmd` 一样加不了执行锁；桥接起它之前验 IP，
  出口不合格时看门狗连桥接一起收。
- **额度是否共享未核实**：Gemini CLI 用同一个 Google 账户登录，与反重力订阅是不是同一个额度池，
  本项目没有核实过。
- **真机端到端未验**（2026-09-20）：面板起 Hub → 汉化生效 → 审批卡自动点选 → 高危命令被拦；
  Gemini CLI 登录 → 酒馆来回一轮。单测与构建过，实机要使用者本人的 Google 账户。

## 0.25.0 GPT 默认归门禁 · 酒馆的内置 GPT 桥接

- **GPT 归门禁的开关反着存。** `settings.json` 里是 `codex_outside_gate`（默认 `false` = 归门禁），
  旧键 `codex_under_gate` 升级后被忽略 —— 0.24 之前手动打开过的人升上来和没开过的人落到同一处：
  都归门禁。要退出只能在「IP 锁」弹窗里点，`settings.json` 里手改旧键没用。
- **GPT 桌面端归门禁之后，Store 包注册失效那条（os error 5）报在 `workspace::launch` 里**，文案一样，
  「修复 Codex 注册」按钮照旧。
- **酒馆的 GPT 桥接没有增量流式。** `codex exec --json` 的 `agent_message` 只有完成事件，
  酒馆开了「流式」也只会整段一起出现。
- **不传图、不给工具。** 请求里的图片段落被丢掉；`-s read-only` 加提示词里明说不要动工具，
  但 Codex 自带的系统提示替换不了，模型偶尔仍会「想去看看文件」—— 角色扮演的质量取决于模型自己，
  面板不替它背书。
- **一次只跑一个 `codex exec`**（酒馆本来也一次只发一发），每个请求冷启动一两秒；超过 300 秒
  的一轮会被结束并报 504。
- **Windows 沙箱首次可能要在 Codex 里完成 setup。** 桥接跑的是槽位 `home` 下的 CLI，那个 `home`
  的 `.sandbox` 没初始化过时，Codex 可能拒绝执行或要求先完成 Windows setup —— 到 GPT 页起一次
  桌面端把 setup 走完，再起桥接。报错原文会原样出现在酒馆的错误里。
- **酒馆那边的连接要自己配**（Custom / OpenAI-compatible，地址 `http://127.0.0.1:5002/v1`，
  密钥点「复制密钥」）。面板不改酒馆的 `settings.json` / `secrets.json`。
- **默认 `--ephemeral`，用量卡不数这些对话。** 打开「记入槽位用量」后写进 `home\sessions`，
  用量卡会数到，但对话也会出现在 Codex 的本机会话历史里。
- **政策线没有结论。** OpenAI 对「第三方前端经官方 CLI 用 ChatGPT 登录」没有公开的明确条款；
  桥接只能长成「本机酒馆调本机 CLI、不碰令牌」这个形状，是否合规由使用者自行判断（DISCLAIMER §8）。
- **Codex 桌面端云端任务不写本机文件**，所以「GPT 槽位的用量」只用过云端任务时是 0 份文件、0 个会话，
  页脚会写明，不是统计坏了（见「账户与用量」一节）。

## 0.24.0 → 0.24.7 官方 Codex turn-state 识别

- **真实「拿 292」只能使用者本人实测。** 单测、离线/回环端到端都过了；但官方 Codex
  （ChatGPT 登录）真的走本机路由采集/注入 `X-Codex-Turn-State`，取决于使用者自己的账号、
  网络与 OpenAI 当时的行为。ccodex 自己都常采不到合格 292。**这是实验功能，默认关闭，
  不保证有效、不保证「满血」、不增加额度。**
- **识别期间别切槽位。** 0.24.7 起识别直接改当前激活槽位的 `config.toml`，marker 记着接管的
  是哪个槽位；切到别的槽位再点「开启」会被拒绝（先关闭再开），切槽位本身不会自动关闭识别 ——
  原槽位的配置要等你点「关闭识别」才恢复。
- **开启后要（重）启动 Codex。** 客户端只在启动时读一次配置，已经开着的 Codex 不会自己改走
  路由 —— 弹窗里已就此提示；关闭识别后同理，开着的那个会收到 503 直到重启。
- **接管期间 Codex 若自己改写了 `config.toml`**（例如重新登录、改模型设置），识别会静默失效：
  路由上没有它的请求，状态表一直是空的。弹窗里「重新应用到当前槽位」可以重放一遍；关闭时只把
  仍是 `qb_turnstate` 的 `model_provider` 改回去，Codex 自己改成别的就不动。
- **面板异常退出（崩溃 / 被杀）时 marker 留着**：那个槽位的 Codex 会对着死端口报错，直到下次
  启动面板自动关闭并恢复。等不及的话，手工把 `config.toml` 里的 `model_provider` 改回
  `config.toml.qb-turnstate-backup` 里的值（或删掉这一行）即可。
- **0.24.0–0.24.6 装过「接入并启动 Codex」的人**：升级后首次启动会自动删掉旧的
  `qb-router-codex-official` 环境记录与目录（连同当时复制进去的 OAuth 快照）；那份复制件若已
  经轮换过刷新令牌、把槽位登出了，到「官方账户 · Codex」重新登录一次即可。
- **只对 Codex。** Claude Code / 桌面端没有这个入口，也不进官方模式（Anthropic 协议里
  没有 turn-state 对应物）。
